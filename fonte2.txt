#include <stdio.h>
#define MAX 8

void main() {
   int i

   int 2x;

   int acumulador = 0.;

   for (i = 1 i <= MAX; i++) {
      if (i % 2 == 0 {
         acumulador = i
         print(acumulador);
      } else
         scan(i);
         "numero impar encontrado
      };
   }

   'a

   return;
};
